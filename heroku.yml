setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    NAME: VICTOR
build:
  docker:
    web: Dockerfile
run:
  web: 
    command: npm run start
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules
